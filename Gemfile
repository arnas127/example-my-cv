source "https://rubygems.org"

# GitHub Pages runs Jekyll in "safe mode" with a pinned dependency set.
# Using the github-pages gem locally keeps your environment aligned.
gem "github-pages", group: :jekyll_plugins
